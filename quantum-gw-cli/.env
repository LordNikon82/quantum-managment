# Quantum Gateway CLI MCP sample configuration
# Copy this file and adjust values to match your environment before running docker compose.

# === Transport ports ===
# Port exposed inside the container.
GW_CLI_TRANSPORT_PORT=3003
# Host port published by docker compose (change if host port differs from transport port).
GW_CLI_PUBLISHED_PORT=3003

# === Target Management ===
# On-premises Security Management / MDS host (hostname or IP) and port.
MANAGEMENT_HOST=your-mgmt-hostname
MANAGEMENT_PORT=443

# === Optional authentication helpers ===
# Username to prefill for login prompts when using username/password auth.
GW_CLI_USERNAME=

# === Optional extras ===
# Prefix automatically added to tool names when registering with MCP.
GW_CLI_TOOL_PREFIX=gw-cli__
# Cloud infra token for Smart-1 Cloud integrations (leave blank if not used).
GW_CLI_CLOUD_INFRA_TOKEN=
# Enable verbose logging for troubleshooting by setting to true.
# DEBUG=true
