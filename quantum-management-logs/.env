# Management Logs MCP sample configuration
# Copy this file and adjust values to match your environment before running docker compose.

# === Management target ===
# Hostname or IP address of your Check Point management server.
MANAGEMENT_HOST=your-mgmt-hostname
# Port for Management API connections.
MANAGEMENT_PORT=443

# === Transport ports ===
# Port exposed inside the container for the MCP HTTP transport.
MCP_HTTP_PORT=3002
# Host port published by docker compose (change if host port differs from MCP_HTTP_PORT).
MCP_PUBLISHED_PORT=3002

# === Optional settings ===
# Enable verbose logging for troubleshooting by setting to true.
# DEBUG=true
# Prefix tool names to avoid collisions when running multiple MCP servers.
# TOOL_PREFIX=logs__
