# Quantum MCP all-in-one suite sample configuration
# Copy this file and adjust the values to match your environment before running docker compose.

# === Service toggles ===
# Set to false to skip starting a specific MCP server inside the combined container.
ENABLE_MANAGEMENT=true
ENABLE_GW_CLI=true
ENABLE_LOGS=true

# === Shared management settings ===
# These values are used as defaults for every MCP server. You can override them per service below.
MANAGEMENT_HOST=
MANAGEMENT_PORT=443
# Uncomment to use Smart-1 Cloud instead of MANAGEMENT_HOST/PORT.
# S1C_URL=https://api.smart-1-cloud.example

# === Shared authentication ===
# Username shared by default across services when authenticating with username/password.
USERNAME=
# Common Smart-1 Cloud token for services that support it.
CLOUD_INFRA_TOKEN=

# === Management MCP overrides ===
# Network ports (inside container vs published host port).
MGMT_TRANSPORT_PORT=3012
MGMT_PUBLISHED_PORT=3012
# Prefix prepended to the MCP tools exposed by the management server.
MGMT_TOOL_PREFIX=mgmt__
# Optional service-specific overrides for shared settings above.
MGMT_USERNAME=
MGMT_MANAGEMENT_HOST=
MGMT_MANAGEMENT_PORT=
MGMT_S1C_URL=
MGMT_CLOUD_INFRA_TOKEN=

# === Gateway CLI MCP overrides ===
GW_TRANSPORT_PORT=3003
GW_PUBLISHED_PORT=3003
GW_TOOL_PREFIX=
# Prefix used when exposing the CLI commands as MCP tools (defaults to gw-cli__).
GW_CLI_TOOL_PREFIX=gw-cli__
GW_USERNAME=
GW_CLI_USERNAME=
GW_MANAGEMENT_HOST=
GW_MANAGEMENT_PORT=
GW_CLOUD_INFRA_TOKEN=
# Optional path to an SSH key file available inside the container.
GW_SSH_KEY_FILE=

# === Management Logs MCP overrides ===
LOGS_TRANSPORT_PORT=3002
LOGS_PUBLISHED_PORT=3002
LOGS_TOOL_PREFIX=logs__
LOGS_MANAGEMENT_HOST=
LOGS_MANAGEMENT_PORT=
