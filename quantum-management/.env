# Quantum Management MCP sample configuration
# Copy this file and adjust values to match your environment before running docker compose.

# === Management target ===
# Choose ONE of the options below:
# 1) On-premises Security Management / MDS host (hostname or IP) and port.
MANAGEMENT_HOST=your-mgmt-hostname
MANAGEMENT_PORT=443
# 2) Smart-1 Cloud API base URL (uncomment to use instead of MANAGEMENT_HOST/PORT).
# S1C_URL=https://api.smart-1-cloud.example

# === Authentication ===
# Username for Management login when using username/password auth.
# Leave blank (USERNAME=) when authenticating via API key only.
USERNAME=your-admin-username

# === Transport ports ===
# Port exposed inside the container.
MCP_TRANSPORT_PORT=3012
# Host port published by docker compose (change if host port differs from transport port).
MCP_PUBLISHED_PORT=3012

# === Optional settings ===
# Enable verbose logging for troubleshooting by setting to true.
# DEBUG=true
